module.exports = {
    content: [
        "./**/*.cshtml",   // Include all Razor views
        "./**/*.html",     // Include HTML files
        "./**/*.razor"     // Include Blazor components (if used)
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}